<h2>プロフィール編集</h2>

<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, label: 'ニックネーム', required: true, autofocus: true %>
    <%= f.input :number_of_bonsai, label: '手入れしている盆栽の数', required: true, autofocus: true %>
    <%= f.input :profile, label: 'プロフィール', required: true, autofocus: true %>
    <%= f.input :email, label: 'メールアドレス', required: true, autofocus: true %>
    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <p>次のメールアドレスが変更承認を待っています。: <%= resource.unconfirmed_email %></p>
    <% end %>
    <p>パスワードを変更しない場合、下記の変更パスワードに何も入力しないでください。</p>
    <%= f.input :password, label: 'パスワード', autocomplete: "off", required: false %>
    <%= f.input :password_confirmation, label: 'パスワード確認', required: false %>
    <%= f.input :current_password, label: '現在のパスワード', hint: "内容を変更するために現在のパスワードを入力してください。", required: true %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "変更" %>
  </div>
<% end %>

<h3>退会する</h3>

<p>退会する場合、<%= link_to "こちら", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %>からお手続きお願いします。</p>

<%= link_to "戻る", :back %>